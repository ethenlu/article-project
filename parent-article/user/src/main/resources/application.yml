server:
  port: 10086

spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/breaking_news?serverTimezone=GMT%2B8
    driver-class-name: com.mysql.cj.jdbc.Driver

  redis:
    database: 1
    host: localhost
    port: 6379
    jedis:
      pool:
        max-active: 20
    lettuce:
      shutdown-timeout: 1000

  data:
    mongodb:
      uri: mongodb://localhost:27017/test

#mybatis-plus:
#  configuration:
#    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl


